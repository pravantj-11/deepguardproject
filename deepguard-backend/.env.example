# DeepGuard is now fully free — no paid API keys required!
# Generate DEEPGUARD_API_KEY:
#   python -c "import secrets; print(secrets.token_urlsafe(32))"

DEEPGUARD_API_KEY=your-random-key-here
DATABASE_URL=sqlite:///./deepguard.db
ALLOWED_ORIGINS=*
